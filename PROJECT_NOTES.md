# HWPX EPUB Maker 작업 인수인계

이 문서는 다른 Mac이나 새로운 Codex 작업에서 프로젝트 맥락을 빠르게 복원하기 위한 인수인계 문서입니다.

새 작업을 시작할 때 Codex에 다음과 같이 요청하세요.

> `README.md`와 `PROJECT_NOTES.md`를 먼저 읽고 현재 소스를 확인한 뒤 이어서 작업해 줘.

## 프로젝트 목적

커버 이미지와 HWPX 원고를 입력받아 다음 두 파일을 함께 만드는 Apple Silicon용 macOS 앱입니다.

- HWPX에서 추출한 TXT
- 커버, 본문, 판권을 포함한 EPUB

앱 이름은 `HWPX 전자책 변환기`입니다. 사용자의 Mac에 Python이 없어도 빌드된 앱은 실행할 수 있도록 Python 변환 엔진을 PyInstaller 실행 파일로 포함합니다.

## 주요 경로

- `macos/main.swift`: macOS GUI, 입력값 저장, 파일 선택, 중복 파일 대치 확인
- `macos/Info.plist`: 앱 정보와 버전
- `engine/main.py`: HWPX 추출, 템플릿 분기, 판권 처리, 출력 파일명 처리
- `engine/txt_to_epub/`: EPUB 변환 라이브러리 수정본
- `engine/txt_to_epub/html_generator.py`: 본문·목차·연재 회차 XHTML 생성
- `engine/txt_to_epub/css.py`: EPUB 공용 CSS
- `assets/app-icon-v1.png`: 앱 아이콘 원본
- `build_app.sh`: Python 엔진과 Swift 앱을 묶어 `.app` 생성
- `THIRD_PARTY_NOTICES.txt`, `licenses/`: 오픈소스 라이선스 고지

## 템플릿 규칙

### 단행본형

EPUB의 기능 목차와 읽기 순서는 다음과 같습니다.

1. 표지
2. 눈에 보이는 목차 페이지
3. 1장
4. 2장
5. 나머지 장
6. 판권

원고 앞부분의 `목차`와 장 목록은 하나의 목차 페이지로 만들고, 실제 본문은 첫 번째 장 제목부터 분리합니다. 판권은 기능 목차에서 `판권` 항목으로 표시합니다.

### 연재형

별도의 눈에 보이는 목차 페이지는 만들지 않습니다. 기능 목차와 읽기 순서는 다음과 같습니다.

1. 표지
2. 첫 줄의 `작품명 + 화수`
3. 판권

원고 해석 규칙:

- 첫 번째 비어 있지 않은 줄 전체: 작품명과 화수, 기능 목차 제목, EPUB 출력 파일명
- 두 번째 비어 있지 않은 줄: 부제목
- 나머지: 본문

예시:

```text
바람난 아내를 버리고 시골로 내려갔더니 수백억이 들어왔다 025화
달려드는 여자들 (1)

본문...
```

연재형 제목과 부제목에는 색상, 테두리, 배경, 아이콘 같은 장식을 넣지 않습니다. 글자 크기와 굵기만으로 구분합니다.

중요: 연재형 본문 표시 방식은 단행본 본문과 같아야 합니다. 현재 단행본 본문 XHTML에는 공용 CSS 링크가 연결되지 않아 전자책 뷰어의 기본 글꼴(테스트 환경에서는 고딕 계열)과 기본 문단 설정을 사용합니다. 따라서 연재형 본문에도 `font-family`, `text-indent` 등의 CSS를 강제로 추가하지 마세요. 첫째·둘째 줄의 제목과 부제목만 인라인 크기·굵기를 사용합니다.

### 연재 폴더 일괄 처리

- 연재형에서만 사용할 수 있습니다.
- 선택한 폴더 바로 아래의 `.hwpx` 파일만 파일명 순서로 처리합니다.
- 하위 폴더는 재귀적으로 검색하지 않습니다.
- 선택한 커버 한 장과 현재 판권 입력값을 모든 회차에 공통 적용합니다.
- 중복 결과는 시작 전에 `모두 대치` 또는 `건너뛰기`를 선택합니다.
- 완료 후 성공·건너뜀 개수를 표시하고 결과 폴더를 열 수 있어야 합니다.

## 판권 처리

앱에서 다음 항목을 직접 입력할 수 있습니다.

- 제목
- 지은이
- 발행처
- 발행일
- UCI
- 투고메일
- 저작권 문구

입력값이 있으면 원고의 판권 블록을 교체하고, 원고에 판권이 없으면 마지막 페이지에 추가합니다. `투고메일`은 `UCI` 다음, `저작권 문구` 바로 위에 출력합니다. 입력값을 모두 비워두면 TXT 원고 안의 판권 정보를 인식해 사용합니다.

`판권정보 저장하기` 버튼으로 값을 macOS 사용자 설정에 저장합니다. 앱을 종료하고 다시 열어도 유지되어야 합니다. 입력창에서는 붙여넣기를 포함한 macOS 표준 편집 단축키가 동작해야 합니다.

## 커버와 목차

- 커버 이미지는 EPUB 메타데이터에만 등록하지 말고 실제 첫 페이지에도 표시합니다.
- 단행본에는 기능 목차와 별도로 독자가 넘겨 볼 수 있는 목차 XHTML 페이지가 있어야 합니다.
- 기능 목차 순서는 템플릿 규칙과 일치해야 합니다.

## 출력 파일과 중복 처리

- TXT와 EPUB을 같은 출력 폴더에 생성합니다.
- 연재형 파일명은 첫 줄의 `작품명 + 화수`로 생성합니다.
- 같은 이름의 결과 파일이 있으면 앱이 기존 파일을 대치할지 사용자에게 확인합니다.
- 대치를 선택하면 엔진에 `--overwrite`가 전달되어 실제로 TXT와 EPUB을 교체해야 합니다.

## 텍스트 처리 주의점

- 한국어를 중국어로 잘못 판단하거나 중국어 제목을 자동 생성하지 않아야 합니다.
- `&#8212;` 같은 HTML 엔티티 문자열이 본문에 그대로 나타나지 않아야 합니다.
- 기존 엔티티를 먼저 정상 문자로 해석한 뒤 XHTML에 안전하게 이스케이프합니다.
- EPUB 내부 언어 코드는 한국어 원고에서 `ko`를 사용합니다.

## 아이콘

모든 앱 아이콘은 `assets/app-icon-v1.png`를 원본으로 사용합니다. `build_app.sh`가 이 PNG에서 `AppIcon.icns`를 다시 생성하므로 아이콘을 바꾸려면 원본 PNG부터 교체하세요.

## 빌드

현재 빌드 대상은 Apple Silicon(M1 이상) macOS입니다.

```bash
./build_app.sh
```

결과:

```text
dist/HWPX 전자책 변환기.app
```

빌드하는 Mac에는 다음 항목이 필요합니다.

- Python 3
- Xcode Command Line Tools
- 인터넷 연결(최초 Python 패키지 설치 시)

빌드 스크립트는 `.build-venv`를 만들고 PyInstaller, EbookLib, chardet, Pillow를 설치합니다. 완성된 앱 사용자는 Python을 별도로 설치할 필요가 없습니다.

배포 ZIP은 `dist/`를 Git에 커밋하지 말고 GitHub Releases에 첨부하는 방식을 권장합니다. 앱은 Apple Developer ID로 공증되지 않았으므로 다른 사용자는 최초 실행 시 Finder에서 우클릭 후 `열기`가 필요할 수 있습니다.

## 검증 절차

수정 후 최소한 다음을 확인하세요.

1. `python3 -m compileall -q engine`
2. `zsh -n build_app.sh`
3. `plutil -lint macos/Info.plist`
4. 단행본 테스트 EPUB의 기능 목차와 눈에 보이는 목차 확인
5. 연재형 테스트 EPUB에서 `표지 → 작품명 nn화 → 판권` 확인
6. 연재형 본문에 강제 명조체나 강제 들여쓰기가 들어가지 않았는지 XHTML 확인
7. 기존 파일 대치 선택 시 TXT와 EPUB이 실제로 갱신되는지 확인
8. 빌드 후 `codesign --verify --deep --strict` 확인

실제 원고, 커버, 생성된 EPUB과 TXT는 저작권 및 개인정보 때문에 저장소에 커밋하지 마세요.

## 라이선스

EPUB 변환 기능은 `oomol-lab/txt-to-epub-converter`의 수정본을 포함합니다. 관련 MIT 고지와 포함된 Python 라이브러리의 고지는 다음 파일에 있습니다.

- `THIRD_PARTY_NOTICES.txt`
- `licenses/TXT_TO_EPUB_CONVERTER_MIT.txt`
- `licenses/EBOOKLIB_AGPL.txt`
- 기타 `licenses/` 파일

라이선스 파일과 앱 안의 오픈소스 고지 메뉴를 제거하지 마세요. 배포 앱에는 수정된 대응 소스도 계속 포함해야 합니다.

## 업데이트 기록 동기화 규칙

기능이나 동작을 변경할 때 다음 항목을 같은 커밋에 포함합니다.

1. 소스 코드와 `Info.plist` 앱 버전
2. 사용자용 `README.md`
3. 버전별 `CHANGELOG.md`
4. 설계 규칙이 바뀌었다면 `PROJECT_NOTES.md`

검증 후 GitHub `main`에 푸시하여 다른 Mac에서도 코드와 기록이 함께 보이도록 합니다.

## 현재 기준 상태

- 앱 버전: `1.4.1`
- GitHub 저장소: `https://github.com/ahnseha-bit/hwpx2epub`
- 기본 브랜치: `main`
- 마지막 핵심 수정: 판권 입력·저장·출력에 투고메일 항목 추가
